import { TaskTooltipDirective } from './task-tooltip.directive';

describe('TaskTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
