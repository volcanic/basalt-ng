import {NgModule} from '@angular/core';
import {DigestImports} from './digest.imports';
import {DigestProviders} from './digest.providers';

@NgModule({
  imports: [DigestImports],
  declarations: [],
  providers: [DigestProviders]
})
export class DigestModule {
}
